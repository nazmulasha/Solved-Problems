#include <bits/stdc++.h>

using namespace std;

int main()
{
    int tt;
    cin>>tt;
    while(tt--)
    {
        double a,b;
        cin>>a;
        b=a*(a/2.0);
        cout<<fixed<<setprecision(2);
        cout<<b<<endl;
    }

    return 0;
}
